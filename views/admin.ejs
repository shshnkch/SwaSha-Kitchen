<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/styles/admin.css">
    <link rel="stylesheet" href="/styles/global.css">
    <script src="/scripts/darkmode.js" defer></script>
</head>
<body>

    <!-- Admin Registration Container -->
    <div id="admin-registration" class="admin-registration">
        <!-- Registration Header -->
        <div id="registration-header" class="section-header">
            <h1>Admin Registration</h1>
            <h2>Set up an admin account</h2>
        </div>

        <!-- Error Message -->
        <% if (typeof error !== 'undefined') {%> 
            <div id="error-message" class="error">
                <p style="color: red;"><%= error %></p> 
            </div>
        <% } %>

        <!-- Registration Form -->
        <form id="registration-form" class="registration-form" action="/adminSetUp" method="POST">
            <!-- Name Field -->
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" placeholder="Enter your full name" required>
            </div>

            <!-- Email Field -->
            <div class="form-group">
                <label for="email">Email ID</label>
                <input type="email" id="email" name="email" placeholder="Enter your email address" required>
            </div>

            <!-- Password Field -->
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" minlength="8" placeholder="Enter a strong password" required>
            </div>

            <!-- Phone Number Field -->
            <div class="form-group">
                <label for="phoneNumber">Phone Number</label>
                <input type="text" id="phoneNumber" name="phoneNumber" pattern="^\d{10}$" placeholder="Enter your phone number" required>
            </div>

            <!-- Address Field -->
            <div class="form-group">
                <label for="address">Address</label>
                <input type="text" id="address" name="address" placeholder="Enter your exact address" required>
            </div>

            <!-- Submit Button Container -->
            <div class="form-group button-container">
                <button type="submit" class="btn">Create Account</button>
            </div>
        </form>

        <!-- Login Prompt -->
        <div id="login-prompt" class="login-prompt">
            <p>Already have an account? <a href="/user/login">Log In</a></p>
        </div>
    </div>
</body>
</html>